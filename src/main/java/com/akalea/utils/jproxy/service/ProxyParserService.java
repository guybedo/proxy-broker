package com.akalea.utils.jproxy.service;

import java.util.List;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.akalea.utils.jproxy.domain.Proxy;
import com.google.common.collect.Lists;

public class ProxyParserService {

    private final static Logger logger = LoggerFactory.getLogger(ProxyParserService.class);

    public List<Proxy> parse(String content, String format) {
        return Lists.newArrayList();
    }
}
